import logging
import json

DEBUG = False
VALIDATE_RESPONSE = False
LOG_FILE = '/var/log/fuel-stats/collector.log'
LOG_LEVEL = logging.ERROR
LOG_ROTATION = False
LOGGER_NAME = 'collector'
SQLALCHEMY_DATABASE_URI = \
  'postgresql://<%= @psql_user %>:<%= @psql_pass %>@localhost/<%= @psql_db %>'

<% if @filtering_rules.is_a?(Hash) %>
with open("/etc/collector-filter.json") as f:
    FILTERING_RULES = json.load(f)
<% end %>
