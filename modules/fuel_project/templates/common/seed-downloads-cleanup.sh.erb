#!/bin/bash

/usr/bin/find <%= @storage_dirs.join(' ') %> -type f -mtime +<%= @keep_iso_days || 10 %> -name <%= @pattern_to_clean || '\*' %> -delete 2>&1 | /usr/bin/logger -t seed-downloads-cleanup
/usr/bin/seedcleaner.py 2>&1 | /usr/bin/logger -t seed-downloads-cleanup
